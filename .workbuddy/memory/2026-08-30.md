# 2026-08-30 工作记录

- 完成 CRM 项目综合走查（代码走查 + 功能检查 + 竞品对比），报告：`crm_review_report_20260830.md`。
- 关键发现：smart_import.py 两个接口无认证可未登录写库；/debug/users 与 /uploads 无鉴权；leads.py:3226 OCR 路径穿越；Qa.vue v-html XSS；SECRET_KEY 硬编码；Leads.vue 1859 行死代码；backend 下两个孤儿库。
- 项目实际规模远超 readme：后端 25 个路由模块 2.2 万行（裸 SQL + SQLite），前端 36 页面 2.3 万行。
- 已修复全部 7 个严重安全项：smart_import 鉴权、/debug/users 删除、/uploads 加 token、OCR 路径穿越、Qa.vue XSS、SECRET_KEY 随机兜底 + crypto_keys 移出 git 跟踪、backend/Dockerfile 改 gunicorn。
- 本机无 Flask 环境（应用跑在 NAS/Docker），验证方式为 AST 语法检查；git 历史中仍有旧私钥，建议用户轮换 crypto_keys。
- 第二轮：合同上传白名单、前端 401/403 统一处理（api/index.js）、reports.py 死代码、backend 下日志/测试脚本/孤儿库清理。
- 重要更正：Leads.vue 不是死代码，IntelligenceHub.vue:19 以标签页引用——走查代理结论有误，勿再删。
