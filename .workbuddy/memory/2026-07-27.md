# 2026-07-27 工作日志

## CRM 系统代码走查
- 对 CRM 项目（Flask + Vue 3 + SQLite）进行了全面代码走查
- 审查范围：backend/app.py（3135行）、config.py、qa_engine.py、前端 api/router/stores/Layout.vue、Docker 配置、Nginx 配置
- 共发现 24 项问题：5 严重、8 高、7 中、4 低
- 关键发现：
  - P0: config.py 硬编码 LLM API Key；Layout.vue token key 不一致（'token' vs 'crm_token'）；Dashboard SQL 拼接；CORS 配置 *+credentials；GET /api/business 含写操作
  - P1: 缺少登录速率限制；request.json 未处理 None；删除操作缺少权限校验和关联数据清理；搜索接口无数据隔离；Dockerfile 用 exec(open()) 启动；requirements.txt 缺 openpyxl
  - P2: app.py 单文件 3100+ 行；Token 验证重复 40+ 处；SQLite 生产环境；裸 except；调试脚本残留
- 输出报告：code_review_report.html

## CRM 系统代码优化修复
基于走查报告进行了以下修复：

### 已修复的问题
1. Dashboard SQL 安全加固 — build_date_filter 增加 time_range 白名单校验；f-string SQL 改为字符串拼接
2. 合同排序 SQL 加固 — get_contracts 中 f-string ORDER BY 改为字符串拼接
3. 合同删除关联清理 — delete_contract 增加 payment_records 清理，防止孤儿数据
4. 商机恢复权限校验 — restore_business 增加所有者/管理员权限检查
5. 跟进日志删除权限校验 — delete_follow_log 增加所有者/管理员权限检查
6. CORS 环境变量支持 — ALLOWED_ORIGINS 支持 CORS_ALLOWED_ORIGINS 环境变量扩展
7. Dockerfile 优化 — 从 exec(open()) 改为 gunicorn -w 4；补全 COPY config.py 和 qa_engine.py
8. requirements.txt 补全 — 添加 openpyxl 和 gunicorn
9. Nginx 安全加固 — 添加安全头和 client_max_body_size 50m
10. docker-compose.yml 优化 — SECRET_KEY 和 CORS_ALLOWED_ORIGINS 支持环境变量注入
11. 前端 API 超时 — 从 10s 增加到 30s

### 未修复（需后续处理）
- app.py 3100+ 行单文件需按 Blueprint 拆分
- token_required 装饰器已定义但大部分路由仍手动验证
- SQLite 生产环境建议迁移 PostgreSQL
- 调试脚本残留建议清理
